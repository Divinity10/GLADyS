# GLADyS Local Development Configuration
# Copy this file to .env and customize for your environment
# .env is gitignored - your settings stay local

# ============================================================================
# Ollama LLM (required for heuristic pattern extraction)
# ============================================================================
# Named endpoints - define URL and model together
# You can add as many as needed: OLLAMA_ENDPOINT_<NAME> + OLLAMA_ENDPOINT_<NAME>_MODEL
OLLAMA_ENDPOINT_LOCAL=http://localhost:11434
OLLAMA_ENDPOINT_LOCAL_MODEL=gemma3:1b

OLLAMA_ENDPOINT_REMOTE=http://your-server:11435
OLLAMA_ENDPOINT_REMOTE_MODEL=gemma3:4b

# Select which endpoint to use (case-insensitive)
# Just change this one line to switch everything
OLLAMA_ENDPOINT=local

# ============================================================================
# PostgreSQL (Memory service storage)
# ============================================================================
STORAGE_HOST=localhost
STORAGE_PORT=5432
STORAGE_DATABASE=gladys
STORAGE_USER=gladys
STORAGE_PASSWORD=gladys_dev

# ============================================================================
# Test Database (isolated from development database)
# ============================================================================
# Used by pytest to avoid destructive test operations against dev data
# Created automatically by 'make setup'
TEST_DB_URL=postgresql://gladys:gladys_dev@localhost:5432/gladys_test

# ============================================================================
# Service Ports (gRPC listen ports)
# ============================================================================
# Used by CLI tools and dashboard to locate services
# For multi-workspace setups, offset these to avoid conflicts
ORCHESTRATOR_PORT=50050
MEMORY_PYTHON_PORT=50051
MEMORY_RUST_PORT=50052
EXECUTIVE_PORT=50053
DB_PORT=5432
DASHBOARD_PORT=8502

# ============================================================================
# Service Addresses (for inter-service communication)
# ============================================================================
# Dashboard and CLI use these to connect to running services
ORCHESTRATOR_ADDRESS=localhost:50050
MEMORY_ADDRESS=localhost:50051
SALIENCE_ADDRESS=localhost:50052
EXECUTIVE_ADDRESS=localhost:50053

# ============================================================================
# Multi-Workspace (for parallel agent development)
# ============================================================================
# Each agent workspace gets its own clone with unique ports and database.
# Use 'make workspace-create NAME=codex' to create a workspace automatically.
#
# Example offset scheme:
#   Primary:  ports 50050-50053, dashboard 8502, DB gladys
#   codex:    ports 50250-50253, dashboard 8702, DB gladys_codex
#   gemini:   ports 50350-50353, dashboard 8802, DB gladys_gemini
#
# The workspace script generates the correct .env for each workspace.
