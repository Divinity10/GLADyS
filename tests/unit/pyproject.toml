[project]
name = "gladys-unit-tests"
version = "0.1.0"
description = "GLADyS Unit Tests"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
packages = []
